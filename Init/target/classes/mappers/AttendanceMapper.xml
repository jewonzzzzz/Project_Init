<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.Init.mapper.AttendanceMapper">
	<!-- namespace를 사용해서 mapper 파일을 찾아오는 주소 설정 -->
	
	<!-- 모든 시간 조회 -->
	<select id="getAllTime" resultType="String">
		select check_in, check_out, workingoutside_time, return_time, overtime,
		       working_time, night_work_time, special_working_time
		from attendance_ch
		where emp_id = #{emp_id}
	</select>
	
</mapper>